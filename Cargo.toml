[package]
name = "cnc_kalkulacka"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
eframe = { version = "0.22.0", default-features = false, features = ["glow", "android-native-activity"] }
# Logger jsme smazali, aby nedělal problémy
android-activity = { version = "=0.4.3", features = ["native-activity"] }

[package.metadata.android]
package = "com.moje.cnckalkulacka"
label = "CNC Kalkulačka"
# DŮLEŽITÉ: Verze 33 je na GitHubu předinstalovaná (nebude hlásit chybu Platform not installed)
android_version = 33
target_sdk_version = 33
min_sdk_version = 23
fullscreen = true
build_targets = ["aarch64-linux-android", "armv7-linux-androideabi"]
